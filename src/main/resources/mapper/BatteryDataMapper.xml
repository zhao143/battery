<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bms.mapper.BatteryDataMapper">

    <resultMap id="BatteryDataPointMap" type="com.example.bms.domain.BatteryDataPoint">
        <id property="id" column="id" />
        <result property="deviceUuid" column="device_uuid" />
        <result property="dataTypeId" column="data_type_id" />
        <result property="dataValue" column="data_value" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <insert id="insert" parameterType="com.example.bms.domain.BatteryDataPoint">
        INSERT INTO battery_data(device_uuid, data_type_id, data_value, created_at)
        VALUES (#{deviceUuid}, #{dataTypeId}, #{dataValue}, #{createdAt})
    </insert>

    <select id="selectLatestByUuidAndType" resultMap="BatteryDataPointMap">
        SELECT * FROM battery_data
        WHERE device_uuid = #{deviceUuid} AND data_type_id = #{dataTypeId}
        ORDER BY created_at DESC, id DESC LIMIT 1
    </select>

    <select id="selectRecentByUuidAndType" resultMap="BatteryDataPointMap">
        SELECT * FROM battery_data
        WHERE device_uuid = #{deviceUuid} AND data_type_id = #{dataTypeId}
        ORDER BY created_at DESC LIMIT #{limit}
    </select>

    <select id="selectDistinctDeviceUuids" resultType="java.lang.String">
        SELECT device_uuid FROM battery_data GROUP BY device_uuid ORDER BY MAX(created_at) DESC
    </select>

</mapper>